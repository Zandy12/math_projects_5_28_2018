'''Prime Factorization - Have the user enter a number and find all Prime
Factors (if there are any) and display them.'''

#factor = []

def prime_check(x):

    for a in range(1,100):
        f = x/a
        if a == x:
            pass
        elif a == 1:
            pass
        elif f % 2 == 1:
            for i in range(1,100):
                b = a/i
                if b % 2 == 1:
                    factor.append(i)
        else:
            pass

    print(factor)

Quit = False

while not Quit:
    factor = []
    number = int(input('What number would you like to see factored? '))
    prime_check(number)
